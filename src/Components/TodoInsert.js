import React from "react";

const TodoInsert = () => {
  return (
    <div>
      <input placeholder="todolist add.."></input>
      <button>추가</button>
    </div>
  );
};

export default TodoInsert;
